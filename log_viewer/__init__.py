"""
Enhanced log viewer package with modular architecture.

Public API:
    - searcher.search(path, keyword, context=0) -> dict
    - formatters.text.format(report) -> str
    - formatters.json_fmt.format(report) -> str
    - formatters.pretty.format(report) -> str
"""

__version__ = "1.0.0"
